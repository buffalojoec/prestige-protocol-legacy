import { FC } from "react";
import { EventWrapper } from "prestige-protocol";


interface EventCardProps {
    event: EventWrapper,
}


export const EventCard: FC<EventCardProps> = (props: EventCardProps) => {
    return (
        <div></div>
    )
}